# 多机位初标与独立边界接续记录

状态：progress / 未完成。范围仅为阶段 0/1 独立标注和边界审阅。按用户要求，验证集冻结、
质量基线、版本化验收配置、用户实测节时四项门禁均保持未通过。没有使用推理、轨迹或自动
回合结果生成真值，也没有把测试通过转换成质量放行。

## 当前材料计数

| 包 | 已保存助手初标 | 待标 | 本次 c/d/e 新增 |
| --- | ---: | ---: | ---: |
| `2_tuning` | 394 / 901 | 507 | 0 |
| `3_tuning` | 532 / 722 | 190 | 128 |
| `2_acceptance` | 450 / 721 | 271 | 450 |
| `3_acceptance` | 111 / 721 | 610 | 111 |
| `IMG_0171_tuning` | 95 / 719 | 624 | 95 |
| `IMG_0171_acceptance` | 144 / 719 | 575 | 144 |
| 合计 | 1,726 / 4,503 | 2,777 | 928 |

这些都是署名 `assistant_visual_review` 的初标，未经用户或独立第二审阅者确认。
已查看但无法定位的帧仍空白并保留原因，不计入完成。初始颜色裁剪、测量草稿和自查图片
均不是第二套生产检测器，也不是独立双人标注。

## 本次实际增量

- `3_tuning` c 批：审阅 f480–721，新增 128 帧（87 可见、41 明确遮挡），其余 114 帧待定。
- 四包 d 批：新增 573 帧。自查撤回 5 个只露出少量球面、无法可靠定位的候选。
- `2_acceptance` e 批：审阅 f240–480，新增 227 帧（98 可见、129 明确遮挡/出画），
  14 帧待定：242、255–259、262–266、318–320。自查撤回 6 个候选、修正 11 个测量点。
  f481–720 仅已准备导航材料，尚未完成独立逐帧审阅，不能算完成。
- 多数球心记录主观估计误差 8 px；e 批 f321/324/326/458 为 12 px，f331 为 10 px。
  这些不是实测定位精度，也不是已锁定的验收阈值，模糊及部分可见球仍需复核。

球在 e 批飞到右上方，且靠近镜头后变大。原区域裁剪与半径上限提示均会遗漏真实球；
因此补看了原尺寸顶部、右边缘与连续邻帧，并用无半径上限的原导航工具辅助定位。
不可见标签来自明确遮挡/出画上下文，不来自提示缺失。工具没有修改生产引擎或写入标签。

## 独立回合边界

帧级标签之外另行查看手球接触、球场和结束事件上下文，没有从球心序列或自动回合派生边界。

- `IMG_0171_tuning`：f54 手球仍接触，f55（源 PTS 121865.0 ms）清楚离手，登记初确认开始。
- `2_acceptance`：f283 可见手球接触，f284（源 PTS 249467.567 ms，整数 249468 ms）
  清楚分离，登记初确认开始；精确结束事件出画，不填写结束。
- 原疑难结束点仍待定：`2.mp4` 的 129.077 s、146.685 s；`3.mp4` 的 125.326 s。
  球与鞋腿重叠，现有画面无法可靠区分落地与人体遮挡，已请求用户协助或更清楚的画面。
- 本地提取的 `3_tuning_end_context_124700_126200.wav` 未能通过当前接口听取，未识别哨声，
  不构成音频边界证据。弹跳外观、身体放松、颜色提示中断或窗口终点均没有作为假死球依据。

六个 `rallies.json` 全部保持原有 `unreviewed` 与空列表，完整回合包仍为 0；这不是已确认
无回合的负例。本次没有缩短、扩展或替换窗口；以前 `2_tuning` 扩到 150 秒的真实材料和
交界三帧证据保留。其他截断窗口仍需逐个查看相邻原片并记录必要的扩展。

## 已执行的验证

全量源视频/图片/标签验证：`data/validation_work/validation_report.json`，状态 `not_ready`，
0 无效、0 损坏、2,777 未标。没有调用冻结选项，没有冻结材料清单。

`experiments/stage1/annotation_integrity_cde_20260906.json` 记录对照上轮快照的只读审计：
4,503 行帧号/PTS/图片路径/图片哈希一致，798 个既有非空标签全部保留，六个回合文件字节
不变；材料清单字节不变且 `frozen=false`。新增 c/d/e 帧记录覆盖全部 928 个新增标签，
CSV 数值、PTS、审阅哈希链以及引用的原图审阅证据全部一致。

新增 `tests/test_visual_review_helpers.py` 的 4 项合成安全检查，只验证导航/叠点工具的
原尺寸裁剪、不写真值、不覆盖已有输出、拒绝重复手工点和半径过滤行为，不能衡量实际质量。
串行全量测试 92 项通过，48.03 秒；报告为
`experiments/stage1/test_results_truth_cde_20260906.xml`。

检查点：`experiments/stage1/checkpoint_truth_cde_20260906.json`。保存当前源文件、真值初标、
文档和校验信息的内容寻址 ZIP，不覆盖旧快照。生产引擎未修改，没有新增 GPU 推理实验；
检查点重新核验并引用原规则 v3 的真实 CUDA profile、原始检测一致性和生命周期证据。
不将这些工程证据当作独立质量或用户节时结果。

## 接续工作

剩余 2,777 帧与六包回合级真值均未完成。优先继续 `2_acceptance` f481–720，以及已准备
但未完成独立审阅的 `3_acceptance` f240 以后、远机位调参 f160 以后、远机位验收 f176 以后；
同时回查已审范围的部分遮挡和原三处结束疑点。其余已生成裁剪图片只算材料，不算标签。

在证据不足时保留待定，不填插值坐标，不扩大完成计数。四项门禁维持未通过，不在本任务中
生成质量基线、锁定验收配置、代替用户计时或推进阶段 2–4。
