# 第 3 机位独立真值初标进展

日期：2026-09-06。仅阶段 0/1；不修改两份研发合同或验收门槛。

## 本轮结果

`data/validation_work/3_tuning/frames.csv` 新增 404 帧助手视觉初标，其中 277 帧可见球心、
127 帧确认遮挡。来源为 `3.mp4` 的真实、旋转归一原尺寸帧；坐标空间为 1920 × 1080。
同一前 480 帧范围内另有 76 帧疑难点留空；480–721 帧仍未提交标签。没有插值标签、
默认不可见值，或把颜色/检测模型提示直接复制为真值。

| 材料包 | 原尺寸帧数 | 当前初标 | 待标 |
| --- | ---: | ---: | ---: |
| 2_tuning | 901 | 394 | 507 |
| 2_acceptance | 721 | 0 | 721 |
| 3_tuning | 722 | 404 | 318 |
| 3_acceptance | 721 | 0 | 721 |
| IMG_0171_tuning | 719 | 0 | 719 |
| IMG_0171_acceptance | 719 | 0 | 719 |
| 合计 | 4,503 | 798 | 3,705 |

上述“初标”不等于已冻结或正式验收。全部新标注署名 `assistant_visual_review`，
`user_reviewed=false`；清晰点估计定位误差 5 像素，人工重测/模糊或局部遮挡点采用更保守
的 8 像素初步估计，冻结前仍需复查。误差估计不是模型验收阈值。

## 标注依据与修正

- 审阅前 480 帧的连续预览、原尺寸网格及相关完整原图；颜色/几何裁剪只用于定位导航。
  辅助脚本不读 VBallNet 输出、轨迹或自动回合，也不写标签。
- 8 个球心经复查修正。原测量文件曾包含 205–209 帧，因可见球面过少撤回，最终仍为空白。
  原测量文件和已渲染复查图不覆盖，以保留修正过程。
- 127 个不可见标记来自连续原图中明确的前景身体/肢体遮挡；不是因颜色提示缺失，
  也不是仅因球不在局部裁剪内。无法明确判断的行继续待定。
- 每个最终帧号、PTS、坐标、方法、未决帧号及 64 项证据文件 SHA-256 保存于
  `data/validation_work/3_tuning/review_20260906_ab.json`；显式审阅决定保存在
  `experiments/stage1/visual_review/3_tuning_ab_decisions_20260906.json`。
- 额外查看后续 480–527 帧原尺寸材料、528–539 帧导航预览，发现多处持球可见但无颜色
  提示，也有蓝色非球目标提示。记录于 `pending_review_notes_20260906_c.json`，未增加标签。

## 独立回合边界

另行查看发球邻帧 12–17 和疑似结束邻帧 156–167，不从球心或轨迹推导回合。

第 13 帧可见手球接触，第 14 帧存在手指/拖影重叠；第 15 帧、源 PTS 120499.2 ms
可明确区分球与手，作为合同“难判断时取首次明确进入比赛运动”的初步开始候选，整数
边界候选为 120499 ms。结束候选在第 160 帧、125325.511 ms（整数 125326 ms），
但球与鞋腿重叠，不能可靠区分地板接触和鞋腿接触；159–161 帧范围仍未解决。

候选单独保存在 `data/validation_work/3_tuning/boundary_review_20260906_a.json`，
没有写入 `rallies.json`。整个窗口尚未完成独立回合复审；空回合列表不是已确认负例。
不得用画面最低点、回弹趋势、窗口终点或人物反应替代明确结束依据。

## 验证与可复现

- `python -m volleycut.eval.validate_truth data/validation_work` 对三个原视频与全部
  4,503 张图片重做哈希校验：0 无效标注、0 损坏图片；状态 `not_ready`、退出码 2。
  原因仍是 3,705 个未标帧及六个未完成回合包，未执行冻结。
- 对照上一轮内容寻址 ZIP：3_tuning 全部 722 行帧号、PTS、图片路径与图片 SHA-256
  不变，其他 11 份帧/回合标签文件逐字节不变。审阅侧文件所列证据哈希全部通过，
  新增帧与 CSV 一一对应；撤回帧继续为空白。
- 审计输出：`experiments/stage1/verification/annotation_3_ab_20260906.json`。
  CSV 更新前 SHA-256：
  `815cd652a871af72762ca1182a66f06684861175d4da52d3389c31216cfe58a1`；更新后：
  `06034e98f3a48528b1fffef83bbde498ee9d3fef29db33a99f384ab0e71a431d`。
- 新串行全量工程回归：88 passed，47.69 秒。报告：
  `experiments/stage1/test_results_truth_3_ab_20260906.xml`。
- 本轮无引擎代码修改、无新 GPU 推理。检查点沿用并校验
  `verification/rules_v3_strict_cuda_raw` 的实际 GPU 证据、规则 v3 重放与严格取消审计；
  不把历史推理记作本轮实验，不用工程一致性代替检测质量。
- 检查点：`experiments/stage1/checkpoint_truth_3_ab_20260906.json`。旧检查点、旧源快照、
  原片和原始 PNG 均保留。

## 仍未通过的门禁与接续

继续测量 3_tuning 后续帧，复查前段 76 个疑难点及已有模糊/局部遮挡初标；独立解决死球
证据，然后完成其他调参与验收窗口。不能按预测结果改选容易片段、删除难例或混入调参数据。

验证集冻结、首次独立质量基线、版本化验收阈值、生产代理帧率选择以及用户实际复核节时
证据仍未完成。阶段 0/1 未达完整验收；本轮是可审计的标注进展，不是质量 Go 判断。
