# CI、冻结校验加固与初标 f 接续

本记录仅保留在本地，不上传素材、标注或实验报告。当前状态为 progress / 未完成；
验证集冻结、独立质量基线、版本化验收配置、用户实测节时四项门禁仍未通过。

## 已交付的工程增量

公开克隆此前完整测试有 11 项失败：6 项属于本地真实材料检查，5 项控制流测试被缺失的
真实视频/权重挡在目标逻辑之前。现在明确标记 `local_data`；其余测试改用临时色块视频和
不加载的模型占位文件，并验证无效输入确实到达对应校验逻辑。完整执行或显式选择本地检查
时，材料缺失仍失败，不自动跳过。公开克隆负向验证为 6 失败、0 跳过（预期结果）。

新增 4 项运行时回退策略回归与 41 项人工夹具冻结校验回归。新测试首先揭露 11 个失败用例：
`python -O` 会移除原回合边界断言，空白审阅者/负例上下文可能通过，布尔索引可能被当整数，
空值证据可能直接抛出异常。校验版本 2 改为显式判断并拒绝这些情况；不修改实际材料的冻结状态。
正常有据边界、源/帧/标签绑定、旋转尺寸和禁止覆盖等路径也有正负对照。

本地完整回归 137 项通过，49.84 秒：`experiments/stage1/test_results_ci_truth_f_20260907.xml`。
公开克隆完整工程集合 131 项通过、6 项显式排除。合成夹具只测工程行为，不是质量真值。

公开提交 `8906c18c729952c6cee4a743a7058dfdb3b80797` 包含 14 个源码、测试、CI 和说明文件。
[GitHub Actions 运行 34048196125](https://github.com/rudykon/VolleyMole/actions/runs/34048196125)
已通过；工作流和任务 REST 接口均确认 `completed/success`，安装锁定依赖、公开回归、
工作树未改写步骤均成功。CI 不接触私有数据，也不冒充 GPU 或质量验收。
回执：`experiments/stage1/github_ci_verified_20260907.json`。

## 新版真实 CUDA 复验

沙箱不能访问 NVIDIA 驱动，宿主机只读查询确认 4 张 RTX 3090 可用，驱动 580.173.02。
本次在宿主机运行新版真实 `2.mp4` 工程样例，而非沿用旧引擎指纹：
`experiments/stage1/verification/ci_hardening_cuda_raw_20260907/`。
当前引擎源码 SHA-256 为 `1ba9659ce97c1ac2ce6473ba6899842d15156af1663632496d1cb2940510cdbb`。

真实执行 361 帧，产出 12 条轨迹、3 个候选回合；8774 个 CUDA 节点含 533 个 Conv，
902 个 CPU 节点为 Gather/Cast/Slice/Concat 形状控制。构造和运行时回退均关闭。
独立产物核验确认源 PTS、输出哈希及原始检测与旧版逐字节一致；报告为
`experiments/stage1/verification/ci_hardening_cuda_audit_20260907.json`。
候选数量不是回合准确率；本次没有在未冻结真值上虚构质量基线。
旧规则与生命周期报告保留为历史证据，不声称它们也是本轮新运行。

## 真实逐帧增量

独立查看 `2_acceptance` f481–551 共 71 帧原尺寸及邻帧材料，保存 51 帧助手初标：
46 个可见球心、5 个明确身体完全遮挡。10 个测量点修正后再次叠点检查，4 个草稿点因
手臂/网带严重遮挡撤回，20 帧疑难项保持空白。审阅者为 `assistant_visual_review`，
不是用户或独立第二审阅者；13 个部分可见球记录 12 px 主观估计误差并要求冻结前复查，
其余可见点为 8 px。估计误差不是实测准确率或验收阈值。

审阅记录：`data/validation_work/2_acceptance/review_20260907_f.json`，绑定 22 份证据。
前态备份保留为同目录 `frames.before_20260907_f.csv`。球在持球、递球过程中多次被衣裤、
手臂和网带遮挡；颜色提示误选标语/衣裤，未将提示缺失变成不可见，也未插值填坐标。
本区段不提供此前比赛回合的结束时刻，没有新增回合边界。

| 包 | 当前助手初标 | 待标 |
| --- | ---: | ---: |
| `2_tuning` | 394 / 901 | 507 |
| `3_tuning` | 532 / 722 | 190 |
| `2_acceptance` | 501 / 721 | 220 |
| `3_acceptance` | 111 / 721 | 610 |
| `IMG_0171_tuning` | 95 / 719 | 624 |
| `IMG_0171_acceptance` | 144 / 719 | 575 |
| 合计 | 1,777 / 4,503 | 2,726 |

对照上一 c/d/e 快照，4,503 行身份字段及 1,726 个既有标签均保留，其他五份帧表、六份
回合文件及材料清单完全不变。目标 CSV 的未审阅前后区段字节也未改写。
`experiments/stage1/annotation_integrity_f_20260907.json` 记录核验结果。
新版全量源视频/图片验证仍为 `not_ready`：0 无效、0 损坏、2,726 未标，完整回合包 0。

## 检查点与后续

检查点：`experiments/stage1/checkpoint_ci_truth_f_20260907.json`。快照现纳入 CI 工作流、
根引擎说明和忽略规则，连同源码/标注/新 CUDA 与 CI 回执一起绑定，不覆盖历史快照。

继续 `2_acceptance` f552–720、其他机位未完成区段、部分球面复审及独立结束事件上下文。
三处原疑难结束点与验收区段出画结束问题尚未解决，不能把捡球、弹跳或窗口终点补成死球。
完成真实真值冻结和基线后才制定锁定验收配置；质量阈值通过后才接通最小网页复核/导出，
最后仍需用户真实计时。当前没有接通网页闭环，也没有替用户进行节时验收。
