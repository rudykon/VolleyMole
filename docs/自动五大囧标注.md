# 自动五大囧标注首批记录

用户于2026-09-12改为要求模型自动标注，不使用真人。该授权取代此前禁止助手标注的要求。原公开动作标签和人评基准保持原文件，新标签使用独立模型schema。

外部视觉API首轮6条均返回未知，追加一轮后1条未知、5条超时，均保留原记录。随后Codex直接检查6条公开VNL-STES原验证素材的24张实际时间采样帧，生成6条独立的模型视觉初筛记录；未确认可入选五大囧的片段。此最终初筛每段只有4帧，不能当作完整过程理解。字段包含实际帧引用、直接观察、6维0–4/null评分、阶段源时间和不确定性。当前声音关联均未知，模型评分不能用来声称独立准确率。

查看 结果页面（本地记录：`runs/auto_bloopers_10min/codex_visual/report/index.html`）、标签JSON（本地记录：`runs/auto_bloopers_10min/codex_visual/annotations.json`） 和 执行记录（本地记录：`runs/auto_bloopers_10min/codex_visual/summary.json`）。自动标注入口为 `scripts/auto_annotate_bloopers.py`，离线报告入口为 `scripts/render_auto_blooper_labels.py`。3项自动标签合同测试通过。

本地实际比赛另外完成全场1915.03秒解码音频扫描，得到8个待视觉核实的声学候选；见 声音候选（本地记录：`runs/auto_bloopers_10min/audio_candidates.json`）。自动审批拒绝向外部API上传该本地视频，理由为未明确授权披露该素材，未发送本地视频；随后仅对已公开的VNL-STES素材获批执行视觉标注。

从开始到交付超过用户要求的10分钟；未将API本身15.08秒耗时当作整个任务耗时。原始文件、代码和报告都已保留，不要求用户填写人评表。
