# 活力版改动与耗时

日期：2026-09-09。成片：`runs/1-top5/top5_lively.mp4`。

## 本次修改

- 标题改用橙黄、薄荷绿、珊瑚粉搭配深色底和奶油白副标题；排名有滑入动效，文案改为“贴地救球！太拼了”“配合到位，节奏拉满！”等简短表达，不新增得分或胜负判断。
- 片头为五段各 0.8 秒的精彩画面快切，总长 4 秒。
- 每个完整回合后加入一次短回放：已评审关键帧附近约 2 秒源画面，以 2/3 速播放 3 秒；显式显示“再看一次 / 0.67×”，原声同步降速且保持音高。
- 片头至正片、回合之间采用 0.4 秒斜向擦除连接动画，配轻量合成提示音。没有加入背景音乐。
- 五个完整回合的顺序、剪辑范围和原声保持不变。原版 `top5.mp4` 未覆盖，SHA-256 仍为 `f692bfb37f89404da825291d5d55e29a6c67e2c1085784130a6c6171b82d8c50`。

新片总长 **123.5 秒**：完整回合 102.5 秒 + 快切片头 4 秒 + 五次回放 15 秒 + 五次连接动画 2 秒。输出为 720×1280、30 fps、3,705 帧、H.264/AAC。

## 一次实际制作耗时

执行命令：

```bash
python tools/volleyball-top-plays/run_match.py \
  --video data/样例视频/1.mp4 --top-k 5 --style lively
```

本机 Intel Core i9-10900X，FFmpeg 7.0.2，CPU/libx264 编码。输入原片约 31 分 55 秒。本次保留既有分析、球追踪、候选清单、关键帧和大模型排名，只重做呈现与验证。

| 项目 | 本次墙钟耗时 |
| --- | ---: |
| 源文件准备、缓存及输入校验等 | 8.46 秒 |
| 新版渲染与合成 | 91.06 秒 |
| 全部片段完整解码、源画面及音频对齐检查 | 17.18 秒 |
| **命令总耗时** | **116.69 秒，约 1 分 57 秒** |

渲染内部计时：完整回合 40.672 秒；快切、慢回放与转场 11.665 秒；最终合并 38.193 秒。内部合计不含子进程启动、报告落盘与哈希登记等少量开销。

这不是从零分析整场比赛的首次耗时，也不包含开发调试时间；历史推理及 API 请求耗时没有重复算入。报告为 `runs/1-top5/timing_lively.json`。随后完全复用成片的恢复检查耗时 8.373 秒，仅写入 `timing_latest.json` 与 `timings/`，没有覆盖实际制作的 116.69 秒记录。

## 验证

- 24 项自动测试通过，包括五/十佳数量、回放源范围、短回合边界、快切长度、时间线连续性、完整回合保留和缓存计时。
- 正式成片及 20 个组成片段完整解码通过，音视频起点与时长一致。
- 五个完整回合的抽样原声在独立片段和合集中偏移均为 0 ms。
- 五个预告与五个慢回放均按源时间与播放速率抽样核对，画面平均绝对误差最大约 5.05；效果片段在合集中的声音位置检查通过。
- 十佳合成测试实际导出 43 秒视频、35 个组成片段，并同时验证 API 故障降级、静音源和无轨迹居中构图。
- 再次运行正式命令全部阶段命中有效缓存，原版文件哈希保持不变。

逐段源时间、播放速率与合集中位置见 `render_report_lively.json`；检查结果见 `verification_lively.json` 与 `alignment_verification_lively.json`。
