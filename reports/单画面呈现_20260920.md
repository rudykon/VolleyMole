# 单画面呈现 · 2026-09-20

按用户要求，正常回合、慢回放和片头快切统一使用单个跟球画面。取消底部全场小窗、分割线及回放边框；保留模板标题和现场原声。经典版与五套活力模板共用的新渲染逻辑均采用此默认方式。

竖屏构图尽量保留源画面高度，避免移除全场小窗的同时额外裁掉落地动作。无有效轨迹时使用居中单画面。默认不追加全场镜头；本轮没有制作额外的全场二次回放。

## 四球预览

沿用用户认可的 9.15 第 2、8 球与 9.8 第 1、3 球，完整回合及 API 慢放首尾保持原值。仅重做构图、提示层与本地导出；没有新增 API 调用，也没有修改生产模型审批记录。

本地交付目录为 `outputs/单画面版_20260920/`，以下文件不随源码发布：

- `index.html`：单播放器预览页，可切换完整版本和慢放速览，并逐球跳转。
- `01_四球完整回合与慢放_单画面.mp4`：3986 帧，132.867 秒。
- `02_四球慢放速览_单画面.mp4`：530 帧，17.667 秒。

两条成片均为 1080×1920、30fps、H.264/AAC；慢放速度为原速的 2/3。该预览复用既有 API 回答，不代表已完成新的模型配置接入或新录像端到端验证。

## 验证

- 全量 582 项离线测试通过；新增真实编码、拼接、声音对齐及错误小窗检测测试，并加入公开 CI。
- 两条成片完整解码、分辨率、总帧数与文件哈希检查通过。
- 完整片 32 个原片裁切对齐比较通过，最大像素平均绝对误差为 3.98/255；两条视频另检查 24 个慢放开头、主触球及结尾样本。
- 逐球原声和拼接音频对齐检查通过；最终慢放接触表已目视检查，未发现分屏或主触球人物被裁掉。
- 四球慢放区间内现有轨迹的 272 个可见球点均位于裁切区域内。这只是既有轨迹检查，不是独立的漏检率或人物完整性保证。

校验改为根据逐帧裁切位置重建原片画面，覆盖画面下半部，不再依赖全场小窗。旧分屏视频仍使用原来的校验路径。

记录位于本地 `runs/single-view-20260920/`：`render_report_lively.json`、`alignment_verification_lively.json`、`media_verification.json`、`tracked_ball_visibility.json` 与 `full-tests-final.log`。
